$("#<%= @section.wrapper_dom_id(params) %>").html("<%= escape_javascript(ratings_for @section, :dimension => params[:dimension], :remote_options => {:url => rate_project_section_path(@section.project, @section)}) %>")