<p>Welcome <%= @resource.email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', main_app.refinery_user_confirmation_url(:locale => I18n.locale, :confirmation_token => @resource.confirmation_token) %></p>
