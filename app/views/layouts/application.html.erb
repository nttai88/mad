<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
<head>
	<meta charset="utf-8" />
	
	<!-- Uncomment to make IE8 render like IE7 -->
	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->
	
	<!-- Set the viewport width to device width for mobile -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	
	<title><%= content_for?(:title) ? yield(:title) : "Company Name" %></title>
  
	<!-- Included CSS Files -->
	<%= stylesheet_link_tag    "application" %>
	<!-- Included JS Files  -->
	<%= javascript_include_tag "application" %>
	<%= render :partial => '/refinery/html_tag' %>
  <% site_bar = render(:partial => '/refinery/site_bar', :locals => {:head => true}) -%>
  <%= render :partial => '/refinery/ie6check' if request.env['HTTP_USER_AGENT'] =~ /MSIE/ -%>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= ajaxful_rating_style %>
    <%#= site_bar -%>
    <div id="container">
      <div id="page_container" class="container row">
        <header id="header">
          <%= render :partial => '/refinery/header' -%>
        </header>
        <div id="showcase">
          <img width="990px" height="110px" alt="" src="/assets/madLab-banner.png">
        </div>
        <div id="shadow-showcase"></div>
        <section id="page">
          <% if ["items", "pages", "inquiries"].include?(controller_name) %>
            <%= yield %>
          <% else %>
            <section id="body_content">
              <h1 id="body_content_title"><%= yield :header %></h1>
              <div id="flash_container">
                <%= render :partial => '/refinery/message' %>
              </div>
              <section id="body_content_left">
                <div class="inner">
                  <%= yield %>
                </div>
              </section>
              <section id="body_content_right">
                <div class="inner">
                  <% if controller_name == "projects" && action_name == "recent" %>
                    <%= render :partial => "layouts/news_side_bar" %>
                  <% else %>
                    <%= render :partial => "layouts/side_bar" %>
                  <% end %>
                </div>
              </section>
            </section>
          <% end %>
        </section>
        <div id="shadow-showcase-mirror"></div>
        <footer>
          <%= render :partial => '/refinery/footer' -%>
        </footer>
      </div>
    </div>
    <%= render :partial => '/refinery/javascripts' %>
  </body>
</html>